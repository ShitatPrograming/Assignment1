#include <stdio.h>
char ceaser(char);
int main(void)
{
    /*
    int n, key, kyn; //kyn=key, yes or no n=typre of cipher
    char code[];
    printf("Enter Message:");
    scanf("%s", &code[]);
    printf("Rotation or Substitution Cipher\n 1= Roation, 2=Substitution:");
    scanf("%d", &n)
    switch(n)
    {
        case 1: //rotation
        printf("Is there a key\n 1=Yes, 2=No:");
        scanf("%d", &kyn)
        switch(kyn)
        {
            case 1:
                printf("Key Value:");
                scanf("%d", &n);
                FUNCTION
                printf
            break;
            case 2:
                Function 
                printf
            break;
            defult:
            printf("Incorrect operation:")
        }
        case 2:
        //sub

        
   
 
   char code[5]="JELLO";//pre-encoded message
   int i ;//index
   int key=1; //need size of
   /*printf("ENTER MESSAGE:") for later use*/
  /*scanf("") for later use*/
   /*prinf("ENTER KEY:") for later use*/
   /*scanf("%d", &key) for later use*/
  for(i=0;i<5;i++)
  {
      if(code[i]>65 && code[i]<90)//if it is a captial letter
      {
      
      code[i]=(code[i]-65+key)%26+65;
      }
  }
  printf("Encrypted message: %s", code);
   
  
  return 0;

}
/*
 * 
 function
 char ceaser(char)
 {
   for(i=0;i<11;i++)
  {
      if(code[i]>65 && code[i]<90)//if it is a captial letter
      {
      
      code[i]=(code[i]-65+key)%26+65;
      }
   }
 }
 
 char deceaser(char)
 {
     for(key=1;key<27;key++)
     {
   for(i=0;i<11;i++)
  {
      if(code[i]>65 && code[i]<90)//if it is a captial letter
      {
      
      code[i]=(code[i]-65-key)%26+65;
      }
  }

   }
 }

 *///
